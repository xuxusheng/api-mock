# API Mock 平台功能设计文档（简化版）

## 1. 项目概述

### 1.1 项目目标
构建一个简单易用的 API Mock 平台，让开发者能够：
- 快速创建和管理 API 模拟接口
- 简单配置响应数据
- 实时预览和调试接口

### 1.2 核心价值
- **提升开发效率**：前后端并行开发，减少依赖等待
- **简化测试流程**：快速构建测试数据
- **即开即用**：无需复杂配置，专注核心功能

## 2. 核心功能模块

### 2.1 项目管理
- **创建项目**：项目名称、描述、基础域名设置
- **项目列表**：查看和管理所有项目
- **项目设置**：修改项目基础信息

### 2.2 接口管理
- **接口配置**
  - 接口路径和HTTP方法（GET/POST/PUT/DELETE）
  - 接口名称和描述
  - 请求参数设置
  - 响应数据配置

- **数据模拟**
  - 静态JSON数据编辑
  - 简单的动态数据生成
  - 多种响应状态码支持

### 2.3 接口调试
- **实时预览**：直接在浏览器中访问Mock接口
- **请求测试**：内置的接口测试工具
- **响应查看**：查看接口返回的数据格式

## 3. 用户界面设计

### 3.1 主要页面
- **工作台页面**：项目列表、创建新项目、快速操作
- **项目详情页**：接口列表、接口分组、项目设置
- **接口编辑页**：接口配置、数据编辑、实时预览

### 3.2 页面布局
- **左侧导航**：项目列表和接口分组
- **主内容区**：接口编辑和配置
- **右侧预览**：实时数据预览

## 4. 数据库设计（简化版）

### 4.1 核心数据模型

```prisma
// 用户表
model User {
  id            String    @id @default(cuid())
  name          String?
  email         String    @unique
  emailVerified DateTime?
  image         String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  
  projects      Project[]
  accounts      Account[]
  sessions      Session[]
}

// 项目表
model Project {
  id          String    @id @default(cuid())
  name        String
  description String?
  baseUrl     String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  
  createdBy   User      @relation(fields: [createdById], references: [id])
  createdById String
  apis        Api[]
  
  @@index([createdById])
}

// 接口表
model Api {
  id          String     @id @default(cuid())
  name        String
  description String?
  path        String
  method      HttpMethod @default(GET)
  config      Json?      // 请求配置
  mockData    Json?      // 模拟数据
  statusCode  Int        @default(200)
  isActive    Boolean    @default(true)
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
  
  project     Project    @relation(fields: [projectId], references: [id])
  projectId   String
  
  @@index([projectId])
  @@index([path, method])
}

enum HttpMethod {
  GET
  POST
  PUT
  DELETE
  PATCH
}
```

## 5. 开发计划

### 5.1 MVP版本（核心功能）
- ✅ 用户认证系统
- 🎯 项目CRUD操作
- 🎯 接口CRUD操作
- 🎯 静态数据模拟
- 🎯 接口实时预览

### 5.2 后续版本
- 🎯 动态数据生成
- 🎯 导入导出功能
- 🎯 接口分组管理
- 🎯 历史版本记录